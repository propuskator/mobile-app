import { StyleSheet } from 'react-native';

// import colors         from '../../../new-assets/constants/colors';

export default (mode) => StyleSheet.create({    // eslint-disable-line no-unused-vars
    containerStyle : {
        // backgroundColor   : colors[mode].BACKGROUND,
        borderWidth       : 0,
        paddingHorizontal : 0,
        marginLeft        : 0,
        width             : 20,
        height            : 20,
        marginRight       : 15
    }
});
