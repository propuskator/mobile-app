import { StyleSheet }    from 'react-native';

export default () => StyleSheet.create({
    defaultIcon : {
        width          : 40,
        height         : 40,
        alignItems     : 'center',
        justifyContent : 'center'
    }
});
